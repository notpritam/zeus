{
  "name": "zeus",
  "version": "1.0.0",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc && cp -r src/renderer dist/",
    "start": "npm run build && electron .",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Remote orchestration tool — headless AI-powered dev server",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "electron": "^41.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
